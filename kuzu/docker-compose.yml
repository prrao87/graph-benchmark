services:
  explorer:
    image: kuzudb/explorer:latest
    restart: unless-stopped
    environment:
      - MODE=READ_ONLY
      - KUZU_FILE=social_network.kuzu
    ports:
      - 8000:8000
    volumes:
      - ./:/database